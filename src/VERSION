#!/bin/sh
# program: dpvs
# May 19, 2025 #
##
# Features
# - dpvs: A brand new Healthcheck that realizes vip/vip-port/backend 3-tier topology, and supports flexible act ioner/checker configurations for each layer.
#
# Bugfixes
# - dpvs: Optimize TOA insertion performance using memmove.
# - dpvs: Fix empty bond offload features.
# - toa: Fixed #1012 allocating 'toa_ip6_sk_lock.lock' before using it.
#

export VERSION=1.9
export RELEASE=10.rc1

echo $VERSION-$RELEASE
